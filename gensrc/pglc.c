/* A packrat parser generated by PackCC 1.2.0 */

#ifdef _MSC_VER
#define _CRT_SECURE_NO_WARNINGS
#endif /* _MSC_VER */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifndef _MSC_VER
#if defined __GNUC__ && defined _WIN32 /* MinGW */
static size_t strnlen(const char *str, size_t maxlen) {
    size_t i;
    for (i = 0; str[i] && i < maxlen; i++);
    return i;
}
#else
#include <unistd.h> /* for strnlen() */
#endif /* defined __GNUC__ && defined _WIN32 */ 
#endif /* _MSC_VER */

#include "pglc.h"


#ifndef PCC_BUFFERSIZE
#define PCC_BUFFERSIZE 256
#endif /* PCC_BUFFERSIZE */

#ifndef PCC_ARRAYSIZE
#define PCC_ARRAYSIZE 2
#endif /* PCC_ARRAYSIZE */

typedef enum pcc_bool_tag {
    PCC_FALSE = 0,
    PCC_TRUE
} pcc_bool_t;

typedef struct pcc_char_array_tag {
    char *buf;
    int max;
    int len;
} pcc_char_array_t;

typedef struct pcc_range_tag {
    int start;
    int end;
} pcc_range_t;

typedef int pcc_value_t;

typedef void *pcc_auxil_t;

typedef struct pcc_value_table_tag {
    pcc_value_t *buf;
    int max;
    int len;
} pcc_value_table_t;

typedef struct pcc_value_refer_table_tag {
    pcc_value_t **buf;
    int max;
    int len;
} pcc_value_refer_table_t;

typedef struct pcc_capture_tag {
    pcc_range_t range;
    char *string; /* mutable */
} pcc_capture_t;

typedef struct pcc_capture_table_tag {
    pcc_capture_t *buf;
    int max;
    int len;
} pcc_capture_table_t;

typedef struct pcc_capture_const_table_tag {
    const pcc_capture_t **buf;
    int max;
    int len;
} pcc_capture_const_table_t;

typedef struct pcc_thunk_tag pcc_thunk_t;
typedef struct pcc_thunk_array_tag pcc_thunk_array_t;

typedef void (*pcc_action_t)(pglc_context_t *, pcc_thunk_t *, pcc_value_t *);

typedef enum pcc_thunk_type_tag {
    PCC_THUNK_LEAF,
    PCC_THUNK_NODE,
} pcc_thunk_type_t;

typedef struct pcc_thunk_leaf_tag {
    pcc_value_refer_table_t values;
    pcc_capture_const_table_t capts;
    pcc_capture_t capt0;
    pcc_action_t action;
} pcc_thunk_leaf_t;

typedef struct pcc_thunk_node_tag {
    const pcc_thunk_array_t *thunks; /* just a reference */
    pcc_value_t *value; /* just a reference */
} pcc_thunk_node_t;

typedef union pcc_thunk_data_tag {
    pcc_thunk_leaf_t leaf;
    pcc_thunk_node_t node;
} pcc_thunk_data_t;

struct pcc_thunk_tag {
    pcc_thunk_type_t type;
    pcc_thunk_data_t data;
};

struct pcc_thunk_array_tag {
    pcc_thunk_t **buf;
    int max;
    int len;
};

typedef struct pcc_thunk_chunk_tag {
    pcc_value_table_t values;
    pcc_capture_table_t capts;
    pcc_thunk_array_t thunks;
    int pos;
} pcc_thunk_chunk_t;

typedef struct pcc_lr_entry_tag pcc_lr_entry_t;

typedef enum pcc_lr_answer_type_tag {
    PCC_LR_ANSWER_LR,
    PCC_LR_ANSWER_CHUNK,
} pcc_lr_answer_type_t;

typedef union pcc_lr_answer_data_tag {
    pcc_lr_entry_t *lr;
    pcc_thunk_chunk_t *chunk;
} pcc_lr_answer_data_t;

typedef struct pcc_lr_answer_tag pcc_lr_answer_t;

struct pcc_lr_answer_tag {
    pcc_lr_answer_type_t type;
    pcc_lr_answer_data_t data;
    int pos;
    pcc_lr_answer_t *hold;
};

typedef pcc_thunk_chunk_t *(*pcc_rule_t)(pglc_context_t *);

typedef struct pcc_rule_set_tag {
    pcc_rule_t *buf;
    int max;
    int len;
} pcc_rule_set_t;

typedef struct pcc_lr_head_tag pcc_lr_head_t;

struct pcc_lr_head_tag {
    pcc_rule_t rule;
    pcc_rule_set_t invol;
    pcc_rule_set_t eval;
    pcc_lr_head_t *hold;
};

typedef struct pcc_lr_memo_tag {
    pcc_rule_t rule;
    pcc_lr_answer_t *answer;
} pcc_lr_memo_t;

typedef struct pcc_lr_memo_map_tag {
    pcc_lr_memo_t *buf;
    int max;
    int len;
} pcc_lr_memo_map_t;

typedef struct pcc_lr_table_entry_tag {
    pcc_lr_head_t *head; /* just a reference */
    pcc_lr_memo_map_t memos;
    pcc_lr_answer_t *hold_a;
    pcc_lr_head_t *hold_h;
} pcc_lr_table_entry_t;

typedef struct pcc_lr_table_tag {
    pcc_lr_table_entry_t **buf;
    int max;
    int len;
} pcc_lr_table_t;

struct pcc_lr_entry_tag {
    pcc_rule_t rule;
    pcc_thunk_chunk_t *seed; /* just a reference */
    pcc_lr_head_t *head; /* just a reference */
};

typedef struct pcc_lr_stack_tag {
    pcc_lr_entry_t **buf;
    int max;
    int len;
} pcc_lr_stack_t;

struct pglc_context_tag {
    int pos;
    pcc_char_array_t buffer;
    pcc_lr_table_t lrtable;
    pcc_lr_stack_t lrstack;
    pcc_auxil_t auxil;
};

#ifndef PCC_ERROR
#define PCC_ERROR(auxil) pcc_error()
static void pcc_error(void) {
    fprintf(stderr, "Syntax error\n");
    exit(1);
}
#endif /* PCC_ERROR */

#ifndef PCC_GETCHAR
#define PCC_GETCHAR(auxil) getchar()
#endif /* PCC_GETCHAR */

#ifndef PCC_MALLOC
#define PCC_MALLOC(auxil, size) pcc_malloc_e(size)
static void *pcc_malloc_e(size_t size) {
    void *p = malloc(size);
    if (p == NULL) {
        fprintf(stderr, "Out of memory\n");
        exit(1);
    }
    return p;
}
#endif /* PCC_MALLOC */

#ifndef PCC_REALLOC
#define PCC_REALLOC(auxil, ptr, size) pcc_realloc_e(ptr, size)
static void *pcc_realloc_e(void *ptr, size_t size) {
    void *p = realloc(ptr, size);
    if (p == NULL) {
        fprintf(stderr, "Out of memory\n");
        exit(1);
    }
    return p;
}
#endif /* PCC_REALLOC */

#ifndef PCC_FREE
#define PCC_FREE(auxil, ptr) free(ptr)
#endif /* PCC_FREE */

static char *pcc_strndup_e(pcc_auxil_t auxil, const char *str, size_t len) {
    size_t m = strnlen(str, len);
    char *s = (char *)PCC_MALLOC(auxil, m + 1);
    memcpy(s, str, m);
    s[m] = '\0';
    return s;
}

static void pcc_char_array__init(pcc_auxil_t auxil, pcc_char_array_t *array, int max) {
    array->len = 0;
    array->max = max;
    array->buf = (char *)PCC_MALLOC(auxil, array->max);
}

static void pcc_char_array__add(pcc_auxil_t auxil, pcc_char_array_t *array, char ch) {
    if (array->max <= 0) array->max = 1;
    while (array->max <= array->len) array->max <<= 1;
    array->buf = (char *)PCC_REALLOC(auxil, array->buf, array->max);
    array->buf[array->len++] = ch;
}

static void pcc_char_array__term(pcc_auxil_t auxil, pcc_char_array_t *array) {
    PCC_FREE(auxil, array->buf);
}

static void pcc_value_table__init(pcc_auxil_t auxil, pcc_value_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_value_t *)PCC_MALLOC(auxil, table->max * sizeof(pcc_value_t));
}

static void pcc_value_table__resize(pcc_auxil_t auxil, pcc_value_table_t *table, int len) {
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_value_t *)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_value_t));
    }
    table->len = len;
}

static void pcc_value_table__term(pcc_auxil_t auxil, pcc_value_table_t *table) {
    PCC_FREE(auxil, table->buf);
}

static void pcc_value_refer_table__init(pcc_auxil_t auxil, pcc_value_refer_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_value_t **)PCC_MALLOC(auxil, table->max * sizeof(pcc_value_t *));
}

static void pcc_value_refer_table__resize(pcc_auxil_t auxil, pcc_value_refer_table_t *table, int len) {
    int i;
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_value_t **)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_value_t *));
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_value_refer_table__term(pcc_auxil_t auxil, pcc_value_refer_table_t *table) {
    PCC_FREE(auxil, table->buf);
}

static void pcc_capture_table__init(pcc_auxil_t auxil, pcc_capture_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_capture_t *)PCC_MALLOC(auxil, table->max * sizeof(pcc_capture_t));
}

static void pcc_capture_table__resize(pcc_auxil_t auxil, pcc_capture_table_t *table, int len) {
    int i;
    for (i = table->len - 1; i >= len; i--) PCC_FREE(auxil, table->buf[i].string);
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_capture_t *)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_capture_t));
    }
    for (i = table->len; i < len; i++) {
        table->buf[i].range.start = 0;
        table->buf[i].range.end = 0;
        table->buf[i].string = NULL;
    }
    table->len = len;
}

static void pcc_capture_table__term(pcc_auxil_t auxil, pcc_capture_table_t *table) {
    int i;
    for (i = table->len - 1; i >= 0; i--) PCC_FREE(auxil, table->buf[i].string);
    PCC_FREE(auxil, table->buf);
}

static void pcc_capture_const_table__init(pcc_auxil_t auxil, pcc_capture_const_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (const pcc_capture_t **)PCC_MALLOC(auxil, table->max * sizeof(const pcc_capture_t *));
}

static void pcc_capture_const_table__resize(pcc_auxil_t auxil, pcc_capture_const_table_t *table, int len) {
    int i;
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (const pcc_capture_t **)PCC_REALLOC(auxil, (pcc_capture_t **)table->buf, table->max * sizeof(const pcc_capture_t *));
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_capture_const_table__term(pcc_auxil_t auxil, pcc_capture_const_table_t *table) {
    PCC_FREE(auxil, (pcc_capture_t **)table->buf);
}

static pcc_thunk_t *pcc_thunk__create_leaf(pcc_auxil_t auxil, pcc_action_t action, int valuec, int captc) {
    pcc_thunk_t *thunk = (pcc_thunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_t));
    thunk->type = PCC_THUNK_LEAF;
    pcc_value_refer_table__init(auxil, &thunk->data.leaf.values, valuec);
    pcc_value_refer_table__resize(auxil, &thunk->data.leaf.values, valuec);
    pcc_capture_const_table__init(auxil, &thunk->data.leaf.capts, captc);
    pcc_capture_const_table__resize(auxil, &thunk->data.leaf.capts, captc);
    thunk->data.leaf.capt0.range.start = 0;
    thunk->data.leaf.capt0.range.end = 0;
    thunk->data.leaf.capt0.string = NULL;
    thunk->data.leaf.action = action;
    return thunk;
}

static pcc_thunk_t *pcc_thunk__create_node(pcc_auxil_t auxil, const pcc_thunk_array_t *thunks, pcc_value_t *value) {
    pcc_thunk_t *thunk = (pcc_thunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_t));
    thunk->type = PCC_THUNK_NODE;
    thunk->data.node.thunks = thunks;
    thunk->data.node.value = value;
    return thunk;
}

static void pcc_thunk__destroy(pcc_auxil_t auxil, pcc_thunk_t *thunk) {
    if (thunk == NULL) return;
    switch (thunk->type) {
    case PCC_THUNK_LEAF:
        PCC_FREE(auxil, thunk->data.leaf.capt0.string);
        pcc_capture_const_table__term(auxil, &thunk->data.leaf.capts);
        pcc_value_refer_table__term(auxil, &thunk->data.leaf.values);
        break;
    case PCC_THUNK_NODE:
        break;
    default: /* unknown */
        break;
    }
    PCC_FREE(auxil, thunk);
}

static void pcc_thunk_array__init(pcc_auxil_t auxil, pcc_thunk_array_t *array, int max) {
    array->len = 0;
    array->max = max;
    array->buf = (pcc_thunk_t **)PCC_MALLOC(auxil, array->max * sizeof(pcc_thunk_t *));
}

static void pcc_thunk_array__add(pcc_auxil_t auxil, pcc_thunk_array_t *array, pcc_thunk_t *thunk) {
    if (array->max <= 0) array->max = 1;
    while (array->max <= array->len) array->max <<= 1;
    array->buf = (pcc_thunk_t **)PCC_REALLOC(auxil, array->buf, array->max * sizeof(pcc_thunk_t *));
    array->buf[array->len++] = thunk;
}

static void pcc_thunk_array__revert(pcc_auxil_t auxil, pcc_thunk_array_t *array, int len) {
    if (array->len > len) {
        int i;
        for (i = array->len - 1; i >= len; i--) pcc_thunk__destroy(auxil, array->buf[i]);
        array->len = len;
    }
}

static void pcc_thunk_array__term(pcc_auxil_t auxil, pcc_thunk_array_t *array) {
    int i;
    for (i = array->len - 1; i >= 0; i--) pcc_thunk__destroy(auxil, array->buf[i]);
    PCC_FREE(auxil, array->buf);
}

static pcc_thunk_chunk_t *pcc_thunk_chunk__create(pcc_auxil_t auxil) {
    pcc_thunk_chunk_t *chunk = (pcc_thunk_chunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_chunk_t));
    pcc_value_table__init(auxil, &chunk->values, PCC_ARRAYSIZE);
    pcc_capture_table__init(auxil, &chunk->capts, PCC_ARRAYSIZE);
    pcc_thunk_array__init(auxil, &chunk->thunks, PCC_ARRAYSIZE);
    chunk->pos = 0;
    return chunk;
}

static void pcc_thunk_chunk__destroy(pcc_auxil_t auxil, pcc_thunk_chunk_t *chunk) {
    if (chunk == NULL) return;
    pcc_thunk_array__term(auxil, &chunk->thunks);
    pcc_capture_table__term(auxil, &chunk->capts);
    pcc_value_table__term(auxil, &chunk->values);
    PCC_FREE(auxil, chunk);
}

static void pcc_rule_set__init(pcc_auxil_t auxil, pcc_rule_set_t *set, int max) {
    set->len = 0;
    set->max = max;
    set->buf = (pcc_rule_t *)PCC_MALLOC(auxil, set->max * sizeof(pcc_rule_t));
}

static int pcc_rule_set__index(pcc_auxil_t auxil, const pcc_rule_set_t *set, pcc_rule_t rule) {
    int i;
    for (i = 0; i < set->len; i++) {
        if (set->buf[i] == rule) return i;
    }
    return -1;
}

static pcc_bool_t pcc_rule_set__add(pcc_auxil_t auxil, pcc_rule_set_t *set, pcc_rule_t rule) {
    int i = pcc_rule_set__index(auxil, set, rule);
    if (i >= 0) return PCC_FALSE;
    if (set->max <= 0) set->max = 1;
    while (set->max <= set->len) set->max <<= 1;
    set->buf = (pcc_rule_t *)PCC_REALLOC(auxil, set->buf, set->max * sizeof(pcc_rule_t));
    set->buf[set->len++] = rule;
    return PCC_TRUE;
}

static pcc_bool_t pcc_rule_set__remove(pcc_auxil_t auxil, pcc_rule_set_t *set, pcc_rule_t rule) {
    int i = pcc_rule_set__index(auxil, set, rule);
    if (i < 0) return PCC_FALSE;
    memmove(set->buf + i, set->buf + (i + 1), (set->len - (i + 1)) * sizeof(pcc_rule_t));
    return PCC_TRUE;
}

static void pcc_rule_set__clear(pcc_auxil_t auxil, pcc_rule_set_t *set) {
    set->len = 0;
}

static void pcc_rule_set__copy(pcc_auxil_t auxil, pcc_rule_set_t *set, const pcc_rule_set_t *src) {
    int i;
    pcc_rule_set__clear(auxil, set);
    for (i = 0; i < src->len; i++) {
        pcc_rule_set__add(auxil, set, src->buf[i]);
    }
}

static void pcc_rule_set__term(pcc_auxil_t auxil, pcc_rule_set_t *set) {
    PCC_FREE(auxil, set->buf);
}

static pcc_lr_head_t *pcc_lr_head__create(pcc_auxil_t auxil, pcc_rule_t rule) {
    pcc_lr_head_t *head = (pcc_lr_head_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_head_t));
    head->rule = rule;
    pcc_rule_set__init(auxil, &head->invol, PCC_ARRAYSIZE);
    pcc_rule_set__init(auxil, &head->eval, PCC_ARRAYSIZE);
    head->hold = NULL;
    return head;
}

static void pcc_lr_head__destroy(pcc_auxil_t auxil, pcc_lr_head_t *head) {
    if (head == NULL) return;
    pcc_lr_head__destroy(auxil, head->hold);
    pcc_rule_set__term(auxil, &head->eval);
    pcc_rule_set__term(auxil, &head->invol);
    PCC_FREE(auxil, head);
}

static void pcc_lr_entry__destroy(pcc_auxil_t auxil, pcc_lr_entry_t *lr);

static pcc_lr_answer_t *pcc_lr_answer__create(pcc_auxil_t auxil, pcc_lr_answer_type_t type, int pos) {
    pcc_lr_answer_t *answer = (pcc_lr_answer_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_answer_t));
    answer->type = type;
    answer->pos = pos;
    answer->hold = NULL;
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        answer->data.lr = NULL;
        break;
    case PCC_LR_ANSWER_CHUNK:
        answer->data.chunk = NULL;
        break;
    default: /* unknown */
        PCC_FREE(auxil, answer);
        answer = NULL;
    }
    return answer;
}

static void pcc_lr_answer__set_chunk(pcc_auxil_t auxil, pcc_lr_answer_t *answer, pcc_thunk_chunk_t *chunk) {
    pcc_lr_answer_t *a = pcc_lr_answer__create(auxil, answer->type, answer->pos);
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        a->data.lr = answer->data.lr;
        break;
    case PCC_LR_ANSWER_CHUNK:
        a->data.chunk = answer->data.chunk;
        break;
    default: /* unknown */
        break;
    }
    a->hold = answer->hold;
    answer->hold = a;
    answer->type = PCC_LR_ANSWER_CHUNK;
    answer->data.chunk = chunk;
}

static void pcc_lr_answer__destroy(pcc_auxil_t auxil, pcc_lr_answer_t *answer) {
    if (answer == NULL) return;
    pcc_lr_answer__destroy(auxil, answer->hold);
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        pcc_lr_entry__destroy(auxil, answer->data.lr);
        break;
    case PCC_LR_ANSWER_CHUNK:
        pcc_thunk_chunk__destroy(auxil, answer->data.chunk);
        break;
    default: /* unknown */
        break;
    }
    PCC_FREE(auxil, answer);
}

static void pcc_lr_memo_map__init(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, int max) {
    map->len = 0;
    map->max = max;
    map->buf = (pcc_lr_memo_t *)PCC_MALLOC(auxil, map->max * sizeof(pcc_lr_memo_t));
}

static int pcc_lr_memo_map__index(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule) {
    int i;
    for (i = 0; i < map->len; i++) {
        if (map->buf[i].rule == rule) return i;
    }
    return -1;
}

static void pcc_lr_memo_map__put(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule, pcc_lr_answer_t *answer) {
    int i = pcc_lr_memo_map__index(auxil, map, rule);
    if (i >= 0) {
        pcc_lr_answer__destroy(auxil, map->buf[i].answer);
        map->buf[i].answer = answer;
    }
    else {
        if (map->max <= 0) map->max = 1;
        while (map->max <= map->len) map->max <<= 1;
        map->buf = (pcc_lr_memo_t *)PCC_REALLOC(auxil, map->buf, map->max * sizeof(pcc_lr_memo_t));
        map->buf[map->len].rule = rule;
        map->buf[map->len].answer = answer;
        map->len++;
    }
}

static pcc_lr_answer_t *pcc_lr_memo_map__get(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule) {
    int i = pcc_lr_memo_map__index(auxil, map, rule);
    return (i >= 0) ? map->buf[i].answer : NULL;
}

static void pcc_lr_memo_map__term(pcc_auxil_t auxil, pcc_lr_memo_map_t *map) {
    int i;
    for (i = map->len - 1; i >= 0; i--) pcc_lr_answer__destroy(auxil, map->buf[i].answer);
    PCC_FREE(auxil, map->buf);
}

static pcc_lr_table_entry_t *pcc_lr_table_entry__create(pcc_auxil_t auxil) {
    pcc_lr_table_entry_t *entry = (pcc_lr_table_entry_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_table_entry_t));
    entry->head = NULL;
    pcc_lr_memo_map__init(auxil, &entry->memos, PCC_ARRAYSIZE);
    entry->hold_a = NULL;
    entry->hold_h = NULL;
    return entry;
}

static void pcc_lr_table_entry__destroy(pcc_auxil_t auxil, pcc_lr_table_entry_t *entry) {
    if (entry == NULL) return;
    pcc_lr_head__destroy(auxil, entry->hold_h);
    pcc_lr_answer__destroy(auxil, entry->hold_a);
    pcc_lr_memo_map__term(auxil, &entry->memos);
    PCC_FREE(auxil, entry);
}

static void pcc_lr_table__init(pcc_auxil_t auxil, pcc_lr_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_lr_table_entry_t **)PCC_MALLOC(auxil, table->max * sizeof(pcc_lr_table_entry_t *));
}

static void pcc_lr_table__resize(pcc_auxil_t auxil, pcc_lr_table_t *table, int len) {
    int i;
    for (i = table->len - 1; i >= len; i--) pcc_lr_table_entry__destroy(auxil, table->buf[i]);
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_lr_table_entry_t **)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_lr_table_entry_t *));
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_lr_table__set_head(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_lr_head_t *head) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    table->buf[index]->head = head;
}

static void pcc_lr_table__hold_head(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_lr_head_t *head) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    head->hold = table->buf[index]->hold_h;
    table->buf[index]->hold_h = head;
}

static void pcc_lr_table__set_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_rule_t rule, pcc_lr_answer_t *answer) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    pcc_lr_memo_map__put(auxil, &table->buf[index]->memos, rule, answer);
}

static void pcc_lr_table__hold_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_lr_answer_t *answer) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    answer->hold = table->buf[index]->hold_a;
    table->buf[index]->hold_a = answer;
}

static pcc_lr_head_t *pcc_lr_table__get_head(pcc_auxil_t auxil, pcc_lr_table_t *table, int index) {
    if (index >= table->len || table->buf[index] == NULL) return NULL;
    return table->buf[index]->head;
}

static pcc_lr_answer_t *pcc_lr_table__get_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_rule_t rule) {
    if (index >= table->len || table->buf[index] == NULL) return NULL;
    return pcc_lr_memo_map__get(auxil, &table->buf[index]->memos, rule);
}

static void pcc_lr_table__shift(pcc_auxil_t auxil, pcc_lr_table_t *table, int count) {
    int i;
    if (count > table->len) count = table->len;
    for (i = 0; i < count; i++) pcc_lr_table_entry__destroy(auxil, table->buf[i]);
    memmove(table->buf, table->buf + count, (table->len - count) * sizeof(pcc_lr_table_entry_t *));
    table->len -= count;
}

static void pcc_lr_table__term(pcc_auxil_t auxil, pcc_lr_table_t *table) {
    int i;
    for (i = table->len - 1; i >= 0; i--) pcc_lr_table_entry__destroy(auxil, table->buf[i]);
    PCC_FREE(auxil, table->buf);
}

static pcc_lr_entry_t *pcc_lr_entry__create(pcc_auxil_t auxil, pcc_rule_t rule) {
    pcc_lr_entry_t *lr = (pcc_lr_entry_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_entry_t));
    lr->rule = rule;
    lr->seed = NULL;
    lr->head = NULL;
    return lr;
}

static void pcc_lr_entry__destroy(pcc_auxil_t auxil, pcc_lr_entry_t *lr) {
    PCC_FREE(auxil, lr);
}

static void pcc_lr_stack__init(pcc_auxil_t auxil, pcc_lr_stack_t *stack, int max) {
    stack->len = 0;
    stack->max = max;
    stack->buf = (pcc_lr_entry_t **)PCC_MALLOC(auxil, stack->max * sizeof(pcc_lr_entry_t *));
}

static void pcc_lr_stack__push(pcc_auxil_t auxil, pcc_lr_stack_t *stack, pcc_lr_entry_t *lr) {
    if (stack->max <= 0) stack->max = 1;
    while (stack->max <= stack->len) stack->max <<= 1;
    stack->buf = (pcc_lr_entry_t **)PCC_REALLOC(auxil, stack->buf, stack->max * sizeof(pcc_lr_entry_t *));
    stack->buf[stack->len++] = lr;
}

static pcc_lr_entry_t *pcc_lr_stack__pop(pcc_auxil_t auxil, pcc_lr_stack_t *stack) {
    return stack->buf[--stack->len];
}

static void pcc_lr_stack__term(pcc_auxil_t auxil, pcc_lr_stack_t *stack) {
    PCC_FREE(auxil, stack->buf);
}

static pglc_context_t *pcc_context__create(pcc_auxil_t auxil) {
    pglc_context_t *ctx = (pglc_context_t *)PCC_MALLOC(auxil, sizeof(pglc_context_t));
    ctx->pos = 0;
    pcc_char_array__init(auxil, &ctx->buffer, PCC_BUFFERSIZE);
    pcc_lr_table__init(auxil, &ctx->lrtable, PCC_BUFFERSIZE);
    pcc_lr_stack__init(auxil, &ctx->lrstack, PCC_ARRAYSIZE);
    ctx->auxil = auxil;
    return ctx;
}

static void pcc_context__destroy(pglc_context_t *ctx) {
    if (ctx == NULL) return;
    pcc_lr_stack__term(ctx->auxil, &ctx->lrstack);
    pcc_lr_table__term(ctx->auxil, &ctx->lrtable);
    pcc_char_array__term(ctx->auxil, &ctx->buffer);
    PCC_FREE(ctx->auxil, ctx);
}

static int pcc_refill_buffer(pglc_context_t *ctx, int num) {
    int n, c;
    n = ctx->buffer.len - ctx->pos;
    if (n >= num) return n;
    while (ctx->buffer.len < ctx->pos + num) {
        c = PCC_GETCHAR(ctx->auxil);
        if (c == EOF) break;
        pcc_char_array__add(ctx->auxil, &ctx->buffer, (char)c);
    }
    return ctx->buffer.len - ctx->pos;
}

static void pcc_commit_buffer(pglc_context_t *ctx) {
    memmove(ctx->buffer.buf, ctx->buffer.buf + ctx->pos, ctx->buffer.len - ctx->pos);
    ctx->buffer.len -= ctx->pos;
    pcc_lr_table__shift(ctx->auxil, &ctx->lrtable, ctx->pos);
    ctx->pos = 0;
}

static const char *pcc_get_capture_string(pglc_context_t *ctx, const pcc_capture_t *capt) {
    if (capt->string == NULL)
        ((pcc_capture_t *)capt)->string =
            pcc_strndup_e(ctx->auxil, ctx->buffer.buf + capt->range.start, capt->range.end - capt->range.start);
    return capt->string;
}

static pcc_bool_t pcc_apply_rule(pglc_context_t *ctx, pcc_rule_t rule, pcc_thunk_array_t *thunks, pcc_value_t *value) {
    static pcc_value_t null;
    pcc_thunk_chunk_t *c = NULL;
    int p = ctx->pos;
    pcc_bool_t b = PCC_TRUE;
    pcc_lr_answer_t *a = pcc_lr_table__get_answer(ctx->auxil, &ctx->lrtable, p, rule);
    pcc_lr_head_t *h = pcc_lr_table__get_head(ctx->auxil, &ctx->lrtable, p);
    if (h != NULL) {
        if (a == NULL && rule != h->rule && pcc_rule_set__index(ctx->auxil, &h->invol, rule) < 0) {
            b = PCC_FALSE;
            c = NULL;
        }
        else if (pcc_rule_set__remove(ctx->auxil, &h->eval, rule)) {
            b = PCC_FALSE;
            c = rule(ctx);
            a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_CHUNK, ctx->pos);
            a->data.chunk = c;
            pcc_lr_table__hold_answer(ctx->auxil, &ctx->lrtable, p, a);
        }
    }
    if (b) {
        if (a != NULL) {
            ctx->pos = a->pos;
            switch (a->type) {
            case PCC_LR_ANSWER_LR:
                if (a->data.lr->head == NULL) {
                    a->data.lr->head = pcc_lr_head__create(ctx->auxil, rule);
                    pcc_lr_table__hold_head(ctx->auxil, &ctx->lrtable, p, a->data.lr->head);
                }
                {
                    int i;
                    for (i = ctx->lrstack.len - 1; i >= 0; i--) {
                        if (ctx->lrstack.buf[i]->head == a->data.lr->head) break;
                        ctx->lrstack.buf[i]->head = a->data.lr->head;
                        pcc_rule_set__add(ctx->auxil, &a->data.lr->head->invol, ctx->lrstack.buf[i]->rule);
                    }
                }
                c = a->data.lr->seed;
                break;
            case PCC_LR_ANSWER_CHUNK:
                c = a->data.chunk;
                break;
            default: /* unknown */
                break;
            }
        }
        else {
            pcc_lr_entry_t *e = pcc_lr_entry__create(ctx->auxil, rule);
            pcc_lr_stack__push(ctx->auxil, &ctx->lrstack, e);
            a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_LR, p);
            a->data.lr = e;
            pcc_lr_table__set_answer(ctx->auxil, &ctx->lrtable, p, rule, a);
            c = rule(ctx);
            pcc_lr_stack__pop(ctx->auxil, &ctx->lrstack);
            a->pos = ctx->pos;
            if (e->head == NULL) {
                pcc_lr_answer__set_chunk(ctx->auxil, a, c);
            }
            else {
                e->seed = c;
                h = a->data.lr->head;
                if (h->rule != rule) {
                    c = a->data.lr->seed;
                    a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_CHUNK, ctx->pos);
                    a->data.chunk = c;
                    pcc_lr_table__hold_answer(ctx->auxil, &ctx->lrtable, p, a);
                }
                else {
                    pcc_lr_answer__set_chunk(ctx->auxil, a, a->data.lr->seed);
                    if (a->data.chunk == NULL) {
                        c = NULL;
                    }
                    else {
                        pcc_lr_table__set_head(ctx->auxil, &ctx->lrtable, p, h);
                        for (;;) {
                            ctx->pos = p;
                            pcc_rule_set__copy(ctx->auxil, &h->eval, &h->invol);
                            c = rule(ctx);
                            if (c == NULL || ctx->pos <= a->pos) break;
                            pcc_lr_answer__set_chunk(ctx->auxil, a, c);
                            a->pos = ctx->pos;
                        }
                        pcc_thunk_chunk__destroy(ctx->auxil, c);
                        pcc_lr_table__set_head(ctx->auxil, &ctx->lrtable, p, NULL);
                        ctx->pos = a->pos;
                        c = a->data.chunk;
                    }
                }
            }
        }
    }
    if (c == NULL) return PCC_FALSE;
    if (value == NULL) value = &null;
    memset(value, 0, sizeof(pcc_value_t)); /* in case */
    pcc_thunk_array__add(ctx->auxil, thunks, pcc_thunk__create_node(ctx->auxil, &c->thunks, value));
    return PCC_TRUE;
}

static void pcc_do_action(pglc_context_t *ctx, const pcc_thunk_array_t *thunks, pcc_value_t *value) {
    int i;
    for (i = 0; i < thunks->len; i++) {
        pcc_thunk_t *thunk = thunks->buf[i];
        switch (thunk->type) {
        case PCC_THUNK_LEAF:
            thunk->data.leaf.action(ctx, thunk, value);
            break;
        case PCC_THUNK_NODE:
            pcc_do_action(ctx, thunk->data.node.thunks, thunk->data.node.value);
            break;
        default: /* unknown */
            break;
        }
    }
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_start(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_single_input(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_file_input(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_eval_input(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_decorator(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_decorators(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_decorated(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_async_funcdef(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_funcdef(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_parameters(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_typedargslist(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_parameter_list(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_parameter_list_posonly(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_parameter_list_no_posonly(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_parameter_list_starargs(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_defparameter(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_parameter(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_identifier(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_expression(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_typec(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_varargslist(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_posarglist(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_varargslist_no_posonly(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_varargslist_starargs(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_vararg(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_simple_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_small_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_expr_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_annassign(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_testlist_star_expr(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_augassign(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_del_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_pass_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_flow_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_break_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_continue_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_return_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_yield_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_raise_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_import_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_import_name(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_import_from(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_import_as_name(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_dotted_as_name(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_import_as_names(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_dotted_as_names(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_dotted_name(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_global_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_nonlocal_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_assert_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_compound_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_async_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_if_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_while_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_for_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_try_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_with_stmt(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_with_item(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_except_clause(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_suite(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_namedexpr_test(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_test(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ordef(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_test_nocond(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_lambdef(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_lambdef_nocond(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_or_test(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_and_test(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_not_test(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_comparison(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_comp_op(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_star_expr(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_expr(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_xor_expr(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_and_expr(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_shift_expr(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_arith_expr(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_term(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_factor(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_power(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_atom_expr(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_atom(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_testlist_comp(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_trailer(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_subscriptlist(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_subscript(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_sliceop(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_exprlist(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_testlist(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_dictorsetmaker(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_classdef(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_arglist(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_argument(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_comp_iter(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_sync_comp_for(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_comp_for(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_comp_if(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_yield_expr(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_func_body_suite(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_name(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NAME(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_number(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NUMBER(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_string(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_STRING(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NEWLINE(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule__(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_INDENT(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DEDENT(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_EQDENT(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ENDMARKER(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_TYPE_COMMENT(pglc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ENCODING(pglc_context_t *ctx);

static pcc_thunk_chunk_t *pcc_evaluate_rule_start(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_file_input, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_eval_input, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_single_input, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0005;
        ctx->pos++;
        ctx->pos = p;
        goto L0000;
    L0005:;
        ctx->pos = p;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_single_input(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_compound_stmt, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NEWLINE, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_simple_stmt, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NEWLINE, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_file_input(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ENCODING, &chunk->thunks, NULL)) goto L0001;
L0001:;
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_stmt, &chunk->thunks, NULL)) goto L0002;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        }
    L0002:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ENDMARKER, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_eval_input(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_testlist, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ENDMARKER, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_decorator(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '@'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_dotted_name, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '('
    ) goto L0001;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_arglist, &chunk->thunks, NULL)) goto L0002;
L0002:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ')'
    ) goto L0001;
    ctx->pos++;
L0001:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NEWLINE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_decorators(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_decorator, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_decorated(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_decorators, &chunk->thunks, NULL)) goto L0000;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_classdef, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_funcdef, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_async_funcdef, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_async_funcdef(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            s[0] != 'a' ||
            s[1] != 's' ||
            s[2] != 'y' ||
            s[3] != 'n' ||
            s[4] != 'c'
        ) goto L0000;
        ctx->pos += 5;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_funcdef, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_funcdef(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            s[0] != 'd' ||
            s[1] != 'e' ||
            s[2] != 'f'
        ) goto L0000;
        ctx->pos += 3;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_parameters, &chunk->thunks, NULL)) goto L0000;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != '-' ||
            s[1] != '>'
        ) goto L0001;
        ctx->pos += 2;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0001;
L0001:;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TYPE_COMMENT, &chunk->thunks, NULL)) goto L0002;
L0002:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_func_body_suite, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_parameters(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '('
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_typedargslist, &chunk->thunks, NULL)) goto L0001;
L0001:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ')'
    ) goto L0000;
    ctx->pos++;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_typedargslist(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_parameter_list, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_parameter_list(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_parameter_list_posonly, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ','
        ) goto L0003;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_parameter_list_no_posonly, &chunk->thunks, NULL)) goto L0004;
    L0004:;
    L0003:;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_parameter_list_no_posonly, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_parameter_list_posonly(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_defparameter, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_typec, &chunk->thunks, NULL)) goto L0002;
        L0002:;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_defparameter, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ','
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_typec, &chunk->thunks, NULL)) goto L0003;
L0003:;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '/'
    ) goto L0000;
    ctx->pos++;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_parameter_list_no_posonly(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_defparameter, &chunk->thunks, NULL)) goto L0002;
        {
            int i;
            for (i = 0;; i++) {
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != ','
                ) goto L0003;
                ctx->pos++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_typec, &chunk->thunks, NULL)) goto L0004;
            L0004:;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_defparameter, &chunk->thunks, NULL)) goto L0003;
            }
        L0003:;
        }
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ','
        ) goto L0005;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_typec, &chunk->thunks, NULL)) goto L0006;
    L0006:;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_parameter_list_starargs, &chunk->thunks, NULL)) goto L0007;
    L0007:;
    L0005:;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_parameter_list_starargs, &chunk->thunks, NULL)) goto L0008;
        goto L0001;
    L0008:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_parameter_list_starargs(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '*' ||
                s[1] != '*'
            ) goto L0002;
            ctx->pos += 2;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_parameter, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ','
        ) goto L0003;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_typec, &chunk->thunks, NULL)) goto L0004;
    L0004:;
    L0003:;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '*'
        ) goto L0005;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_parameter, &chunk->thunks, NULL)) goto L0006;
    L0006:;
        {
            int i;
            for (i = 0;; i++) {
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != ','
                ) goto L0007;
                ctx->pos++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_typec, &chunk->thunks, NULL)) goto L0008;
            L0008:;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_defparameter, &chunk->thunks, NULL)) goto L0007;
            }
        L0007:;
        }
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ','
        ) goto L0009;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_typec, &chunk->thunks, NULL)) goto L0010;
    L0010:;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '*' ||
                s[1] != '*'
            ) goto L0011;
            ctx->pos += 2;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_parameter, &chunk->thunks, NULL)) goto L0011;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ','
        ) goto L0012;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_typec, &chunk->thunks, NULL)) goto L0013;
    L0013:;
    L0012:;
    L0011:;
    L0009:;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_defparameter(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_parameter, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '='
    ) goto L0001;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expression, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_parameter(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_identifier, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0001;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expression, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_identifier(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_expression(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_typec(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TYPE_COMMENT, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_varargslist(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_posarglist, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ','
        ) goto L0003;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_varargslist_no_posonly, &chunk->thunks, NULL)) goto L0004;
    L0004:;
    L0003:;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_varargslist_no_posonly, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_posarglist(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_vararg, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_vararg, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ','
    ) goto L0000;
    ctx->pos++;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '/'
    ) goto L0000;
    ctx->pos++;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_varargslist_no_posonly(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_vararg, &chunk->thunks, NULL)) goto L0002;
        {
            int i;
            for (i = 0;; i++) {
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != ','
                ) goto L0003;
                ctx->pos++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_vararg, &chunk->thunks, NULL)) goto L0003;
            }
        L0003:;
        }
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ','
        ) goto L0004;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_varargslist_starargs, &chunk->thunks, NULL)) goto L0005;
    L0005:;
    L0004:;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_varargslist_starargs, &chunk->thunks, NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_varargslist_starargs(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '*' ||
                s[1] != '*'
            ) goto L0002;
            ctx->pos += 2;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_vararg, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ','
        ) goto L0003;
        ctx->pos++;
    L0003:;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '*'
        ) goto L0004;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_vararg, &chunk->thunks, NULL)) goto L0005;
    L0005:;
        {
            int i;
            for (i = 0;; i++) {
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != ','
                ) goto L0006;
                ctx->pos++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_vararg, &chunk->thunks, NULL)) goto L0006;
            }
        L0006:;
        }
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ','
        ) goto L0007;
        ctx->pos++;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '*' ||
                s[1] != '*'
            ) goto L0008;
            ctx->pos += 2;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_vararg, &chunk->thunks, NULL)) goto L0008;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ','
        ) goto L0009;
        ctx->pos++;
    L0009:;
    L0008:;
    L0007:;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_vararg(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '='
    ) goto L0001;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expression, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_compound_stmt, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_simple_stmt, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_simple_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_small_stmt, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ';'
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_small_stmt, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ';'
    ) goto L0002;
    ctx->pos++;
L0002:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NEWLINE, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_small_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expr_stmt, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_flow_stmt, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_del_stmt, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_pass_stmt, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_global_stmt, &chunk->thunks, NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_nonlocal_stmt, &chunk->thunks, NULL)) goto L0007;
        goto L0001;
    L0007:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_assert_stmt, &chunk->thunks, NULL)) goto L0008;
        goto L0001;
    L0008:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_import_stmt, &chunk->thunks, NULL)) goto L0009;
        goto L0001;
    L0009:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_expr_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_testlist_star_expr, &chunk->thunks, NULL)) goto L0000;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_annassign, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_augassign, &chunk->thunks, NULL)) goto L0003;
        {
            int p = ctx->pos;
            int n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_yield_expr, &chunk->thunks, NULL)) goto L0005;
            goto L0004;
        L0005:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_testlist, &chunk->thunks, NULL)) goto L0006;
            goto L0004;
        L0006:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0003;
        L0004:;
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int i;
            for (i = 0;; i++) {
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '='
                ) goto L0008;
                ctx->pos++;
                {
                    int p = ctx->pos;
                    int n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_yield_expr, &chunk->thunks, NULL)) goto L0010;
                    goto L0009;
                L0010:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_testlist_star_expr, &chunk->thunks, NULL)) goto L0011;
                    goto L0009;
                L0011:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0008;
                L0009:;
                }
            }
        L0008:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TYPE_COMMENT, &chunk->thunks, NULL)) goto L0012;
    L0012:;
        /* unreachable codes omitted */
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_annassign(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '='
    ) goto L0001;
    ctx->pos++;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_yield_expr, &chunk->thunks, NULL)) goto L0003;
        goto L0002;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_testlist, &chunk->thunks, NULL)) goto L0004;
        goto L0002;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0001;
    L0002:;
    }
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_testlist_star_expr(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_star_expr, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0004;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0004;
            {
                int p = ctx->pos;
                int n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0006;
                goto L0005;
            L0006:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_star_expr, &chunk->thunks, NULL)) goto L0007;
                goto L0005;
            L0007:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0004;
            L0005:;
            }
        }
    L0004:;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ','
    ) goto L0008;
    ctx->pos++;
L0008:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_augassign(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '+' ||
                s[1] != '='
            ) goto L0002;
            ctx->pos += 2;
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '-' ||
                s[1] != '='
            ) goto L0003;
            ctx->pos += 2;
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '*' ||
                s[1] != '='
            ) goto L0004;
            ctx->pos += 2;
        }
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '@' ||
                s[1] != '='
            ) goto L0005;
            ctx->pos += 2;
        }
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '/' ||
                s[1] != '='
            ) goto L0006;
            ctx->pos += 2;
        }
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '%' ||
                s[1] != '='
            ) goto L0007;
            ctx->pos += 2;
        }
        goto L0001;
    L0007:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '&' ||
                s[1] != '='
            ) goto L0008;
            ctx->pos += 2;
        }
        goto L0001;
    L0008:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '|' ||
                s[1] != '='
            ) goto L0009;
            ctx->pos += 2;
        }
        goto L0001;
    L0009:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '^' ||
                s[1] != '='
            ) goto L0010;
            ctx->pos += 2;
        }
        goto L0001;
    L0010:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                s[0] != '<' ||
                s[1] != '<' ||
                s[2] != '='
            ) goto L0011;
            ctx->pos += 3;
        }
        goto L0001;
    L0011:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                s[0] != '>' ||
                s[1] != '>' ||
                s[2] != '='
            ) goto L0012;
            ctx->pos += 3;
        }
        goto L0001;
    L0012:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                s[0] != '*' ||
                s[1] != '*' ||
                s[2] != '='
            ) goto L0013;
            ctx->pos += 3;
        }
        goto L0001;
    L0013:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                s[0] != '/' ||
                s[1] != '/' ||
                s[2] != '='
            ) goto L0014;
            ctx->pos += 3;
        }
        goto L0001;
    L0014:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_del_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            s[0] != 'd' ||
            s[1] != 'e' ||
            s[2] != 'l'
        ) goto L0000;
        ctx->pos += 3;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_exprlist, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_pass_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            s[0] != 'p' ||
            s[1] != 'a' ||
            s[2] != 's' ||
            s[3] != 's'
        ) goto L0000;
        ctx->pos += 4;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_flow_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_break_stmt, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_continue_stmt, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_return_stmt, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_raise_stmt, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_yield_stmt, &chunk->thunks, NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_break_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            s[0] != 'b' ||
            s[1] != 'r' ||
            s[2] != 'e' ||
            s[3] != 'a' ||
            s[4] != 'k'
        ) goto L0000;
        ctx->pos += 5;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_continue_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            s[0] != 'c' ||
            s[1] != 'o' ||
            s[2] != 'n' ||
            s[3] != 't' ||
            s[4] != 'i' ||
            s[5] != 'n' ||
            s[6] != 'u' ||
            s[7] != 'e'
        ) goto L0000;
        ctx->pos += 8;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_return_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            s[0] != 'r' ||
            s[1] != 'e' ||
            s[2] != 't' ||
            s[3] != 'u' ||
            s[4] != 'r' ||
            s[5] != 'n'
        ) goto L0000;
        ctx->pos += 6;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_testlist_star_expr, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_yield_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_yield_expr, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_raise_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            s[0] != 'r' ||
            s[1] != 'a' ||
            s[2] != 'i' ||
            s[3] != 's' ||
            s[4] != 'e'
        ) goto L0000;
        ctx->pos += 5;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0001;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            s[0] != 'f' ||
            s[1] != 'r' ||
            s[2] != 'o' ||
            s[3] != 'm'
        ) goto L0002;
        ctx->pos += 4;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0002;
L0002:;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_import_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_import_name, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_import_from, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_import_name(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            s[0] != 'i' ||
            s[1] != 'm' ||
            s[2] != 'p' ||
            s[3] != 'o' ||
            s[4] != 'r' ||
            s[5] != 't'
        ) goto L0000;
        ctx->pos += 6;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_dotted_as_names, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_import_from(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            s[0] != 'f' ||
            s[1] != 'r' ||
            s[2] != 'o' ||
            s[3] != 'm'
        ) goto L0000;
        ctx->pos += 4;
    }
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            int i;
            for (i = 0;; i++) {
                int p = ctx->pos;
                int n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '.'
                ) goto L0005;
                ctx->pos++;
                goto L0004;
            L0005:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                {
                    const char *s = ctx->buffer.buf + ctx->pos;
                    if (
                        pcc_refill_buffer(ctx, 3) < 3 ||
                        s[0] != '.' ||
                        s[1] != '.' ||
                        s[2] != '.'
                    ) goto L0006;
                    ctx->pos += 3;
                }
                goto L0004;
            L0006:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0003;
            L0004:;
            }
        L0003:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_dotted_name, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int i;
            for (i = 0;; i++) {
                int p = ctx->pos;
                int n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '.'
                ) goto L0010;
                ctx->pos++;
                goto L0009;
            L0010:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                {
                    const char *s = ctx->buffer.buf + ctx->pos;
                    if (
                        pcc_refill_buffer(ctx, 3) < 3 ||
                        s[0] != '.' ||
                        s[1] != '.' ||
                        s[2] != '.'
                    ) goto L0011;
                    ctx->pos += 3;
                }
                goto L0009;
            L0011:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0008;
            L0009:;
            }
        L0008:;
        }
    L0001:;
    }
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            s[0] != 'i' ||
            s[1] != 'm' ||
            s[2] != 'p' ||
            s[3] != 'o' ||
            s[4] != 'r' ||
            s[5] != 't'
        ) goto L0000;
        ctx->pos += 6;
    }
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '*'
        ) goto L0013;
        ctx->pos++;
        goto L0012;
    L0013:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '('
        ) goto L0014;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0014;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_import_as_names, &chunk->thunks, NULL)) goto L0014;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0014;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ')'
        ) goto L0014;
        ctx->pos++;
        goto L0012;
    L0014:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_import_as_names, &chunk->thunks, NULL)) goto L0015;
        goto L0012;
    L0015:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0012:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_import_as_name(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0000;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != 'a' ||
            s[1] != 's'
        ) goto L0001;
        ctx->pos += 2;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_dotted_as_name(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_dotted_name, &chunk->thunks, NULL)) goto L0000;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != 'a' ||
            s[1] != 's'
        ) goto L0001;
        ctx->pos += 2;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_import_as_names(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_import_as_name, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_import_as_name, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ','
    ) goto L0002;
    ctx->pos++;
L0002:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_dotted_as_names(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_dotted_as_name, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_dotted_as_name, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_dotted_name(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '.'
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_global_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            s[0] != 'g' ||
            s[1] != 'l' ||
            s[2] != 'o' ||
            s[3] != 'b' ||
            s[4] != 'a' ||
            s[5] != 'l'
        ) goto L0000;
        ctx->pos += 6;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_nonlocal_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 8) < 8 ||
            s[0] != 'n' ||
            s[1] != 'o' ||
            s[2] != 'n' ||
            s[3] != 'l' ||
            s[4] != 'o' ||
            s[5] != 'c' ||
            s[6] != 'a' ||
            s[7] != 'l'
        ) goto L0000;
        ctx->pos += 8;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_assert_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            s[0] != 'a' ||
            s[1] != 's' ||
            s[2] != 's' ||
            s[3] != 'e' ||
            s[4] != 'r' ||
            s[5] != 't'
        ) goto L0000;
        ctx->pos += 6;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ','
    ) goto L0001;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_compound_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_if_stmt, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_for_stmt, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_while_stmt, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_try_stmt, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_with_stmt, &chunk->thunks, NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_funcdef, &chunk->thunks, NULL)) goto L0007;
        goto L0001;
    L0007:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_classdef, &chunk->thunks, NULL)) goto L0008;
        goto L0001;
    L0008:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_decorated, &chunk->thunks, NULL)) goto L0009;
        goto L0001;
    L0009:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_async_stmt, &chunk->thunks, NULL)) goto L0010;
        goto L0001;
    L0010:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_async_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            s[0] != 'a' ||
            s[1] != 's' ||
            s[2] != 'y' ||
            s[3] != 'n' ||
            s[4] != 'c'
        ) goto L0000;
        ctx->pos += 5;
    }
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_funcdef, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_with_stmt, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_for_stmt, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_if_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != 'i' ||
            s[1] != 'f'
        ) goto L0000;
        ctx->pos += 2;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_namedexpr_test, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQDENT, &chunk->thunks, NULL)) goto L0001;
            {
                const char *s = ctx->buffer.buf + ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    s[0] != 'e' ||
                    s[1] != 'l' ||
                    s[2] != 'i' ||
                    s[3] != 'f'
                ) goto L0001;
                ctx->pos += 4;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_namedexpr_test, &chunk->thunks, NULL)) goto L0001;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ':'
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQDENT, &chunk->thunks, NULL)) goto L0002;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            s[0] != 'e' ||
            s[1] != 'l' ||
            s[2] != 's' ||
            s[3] != 'e'
        ) goto L0002;
        ctx->pos += 4;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0002;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0002;
L0002:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_while_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            s[0] != 'w' ||
            s[1] != 'h' ||
            s[2] != 'i' ||
            s[3] != 'l' ||
            s[4] != 'e'
        ) goto L0000;
        ctx->pos += 5;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_namedexpr_test, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQDENT, &chunk->thunks, NULL)) goto L0001;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            s[0] != 'e' ||
            s[1] != 'l' ||
            s[2] != 's' ||
            s[3] != 'e'
        ) goto L0001;
        ctx->pos += 4;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0001;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_for_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            s[0] != 'f' ||
            s[1] != 'o' ||
            s[2] != 'r'
        ) goto L0000;
        ctx->pos += 3;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_exprlist, &chunk->thunks, NULL)) goto L0000;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != 'i' ||
            s[1] != 'n'
        ) goto L0000;
        ctx->pos += 2;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_testlist, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TYPE_COMMENT, &chunk->thunks, NULL)) goto L0001;
L0001:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQDENT, &chunk->thunks, NULL)) goto L0002;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            s[0] != 'e' ||
            s[1] != 'l' ||
            s[2] != 's' ||
            s[3] != 'e'
        ) goto L0002;
        ctx->pos += 4;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0002;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0002;
L0002:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_try_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            s[0] != 't' ||
            s[1] != 'r' ||
            s[2] != 'y'
        ) goto L0000;
        ctx->pos += 3;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0000;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            int i;
            for (i = 0;; i++) {
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQDENT, &chunk->thunks, NULL)) goto L0003;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_except_clause, &chunk->thunks, NULL)) goto L0003;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != ':'
                ) goto L0003;
                ctx->pos++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0003;
            }
        L0003:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQDENT, &chunk->thunks, NULL)) goto L0004;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                s[0] != 'e' ||
                s[1] != 'l' ||
                s[2] != 's' ||
                s[3] != 'e'
            ) goto L0004;
            ctx->pos += 4;
        }
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ':'
        ) goto L0004;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0004;
    L0004:;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQDENT, &chunk->thunks, NULL)) goto L0005;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 7) < 7 ||
                s[0] != 'f' ||
                s[1] != 'i' ||
                s[2] != 'n' ||
                s[3] != 'a' ||
                s[4] != 'l' ||
                s[5] != 'l' ||
                s[6] != 'y'
            ) goto L0005;
            ctx->pos += 7;
        }
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ':'
        ) goto L0005;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0005;
    L0005:;
        /* unreachable codes omitted */
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_with_stmt(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            s[0] != 'w' ||
            s[1] != 'i' ||
            s[2] != 't' ||
            s[3] != 'h'
        ) goto L0000;
        ctx->pos += 4;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_with_item, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_with_item, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TYPE_COMMENT, &chunk->thunks, NULL)) goto L0002;
L0002:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_with_item(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0000;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != 'a' ||
            s[1] != 's'
        ) goto L0001;
        ctx->pos += 2;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expr, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_except_clause(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            s[0] != 'e' ||
            s[1] != 'x' ||
            s[2] != 'c' ||
            s[3] != 'e' ||
            s[4] != 'p' ||
            s[5] != 't'
        ) goto L0000;
        ctx->pos += 6;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0001;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != 'a' ||
            s[1] != 's'
        ) goto L0002;
        ctx->pos += 2;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0002;
L0002:;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_suite(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NEWLINE, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INDENT, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_stmt, &chunk->thunks, NULL)) goto L0002;
        {
            int i;
            for (i = 0;; i++) {
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQDENT, &chunk->thunks, NULL)) goto L0003;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_stmt, &chunk->thunks, NULL)) goto L0003;
            }
        L0003:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DEDENT, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_simple_stmt, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_namedexpr_test(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0000;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != ':' ||
            s[1] != '='
        ) goto L0001;
        ctx->pos += 2;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_test(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_lambdef, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ordef, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ordef(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_or_test, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != 'i' ||
            s[1] != 'f'
        ) goto L0001;
        ctx->pos += 2;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_or_test, &chunk->thunks, NULL)) goto L0001;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 4) < 4 ||
            s[0] != 'e' ||
            s[1] != 'l' ||
            s[2] != 's' ||
            s[3] != 'e'
        ) goto L0001;
        ctx->pos += 4;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_test_nocond(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_or_test, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_lambdef_nocond, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_lambdef(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            s[0] != 'l' ||
            s[1] != 'a' ||
            s[2] != 'm' ||
            s[3] != 'b' ||
            s[4] != 'd' ||
            s[5] != 'a'
        ) goto L0000;
        ctx->pos += 6;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_varargslist, &chunk->thunks, NULL)) goto L0001;
L0001:;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_lambdef_nocond(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 6) < 6 ||
            s[0] != 'l' ||
            s[1] != 'a' ||
            s[2] != 'm' ||
            s[3] != 'b' ||
            s[4] != 'd' ||
            s[5] != 'a'
        ) goto L0000;
        ctx->pos += 6;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_varargslist, &chunk->thunks, NULL)) goto L0001;
L0001:;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test_nocond, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_or_test(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_and_test, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            {
                const char *s = ctx->buffer.buf + ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 2) < 2 ||
                    s[0] != 'o' ||
                    s[1] != 'r'
                ) goto L0001;
                ctx->pos += 2;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_and_test, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_and_test(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_not_test, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            {
                const char *s = ctx->buffer.buf + ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    s[0] != 'a' ||
                    s[1] != 'n' ||
                    s[2] != 'd'
                ) goto L0001;
                ctx->pos += 3;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_not_test, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_not_test(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                s[0] != 'n' ||
                s[1] != 'o' ||
                s[2] != 't'
            ) goto L0002;
            ctx->pos += 3;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_not_test, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_comparison, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_comparison(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expr, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_comp_op, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expr, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_comp_op(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '=' ||
                s[1] != '='
            ) goto L0002;
            ctx->pos += 2;
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '>' ||
                s[1] != '='
            ) goto L0003;
            ctx->pos += 2;
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '<' ||
                s[1] != '='
            ) goto L0004;
            ctx->pos += 2;
        }
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '<' ||
                s[1] != '>'
            ) goto L0005;
            ctx->pos += 2;
        }
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '<'
        ) goto L0006;
        ctx->pos++;
        {
            int p = ctx->pos;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '<'
            ) goto L0007;
            ctx->pos++;
            ctx->pos = p;
            goto L0006;
        L0007:;
            ctx->pos = p;
        }
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '>'
        ) goto L0008;
        ctx->pos++;
        {
            int p = ctx->pos;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '>'
            ) goto L0009;
            ctx->pos++;
            ctx->pos = p;
            goto L0008;
        L0009:;
            ctx->pos = p;
        }
        goto L0001;
    L0008:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '!' ||
                s[1] != '='
            ) goto L0010;
            ctx->pos += 2;
        }
        goto L0001;
    L0010:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                s[0] != 'n' ||
                s[1] != 'o' ||
                s[2] != 't'
            ) goto L0011;
            ctx->pos += 3;
        }
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != 'i' ||
                s[1] != 'n'
            ) goto L0011;
            ctx->pos += 2;
        }
        goto L0001;
    L0011:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != 'i' ||
                s[1] != 'n'
            ) goto L0012;
            ctx->pos += 2;
        }
        goto L0001;
    L0012:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != 'i' ||
                s[1] != 's'
            ) goto L0013;
            ctx->pos += 2;
        }
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                s[0] != 'n' ||
                s[1] != 'o' ||
                s[2] != 't'
            ) goto L0013;
            ctx->pos += 3;
        }
        goto L0001;
    L0013:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != 'i' ||
                s[1] != 's'
            ) goto L0014;
            ctx->pos += 2;
        }
        goto L0001;
    L0014:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_star_expr(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '*'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expr, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_expr(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_xor_expr, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '|'
            ) goto L0001;
            ctx->pos++;
            {
                int p = ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '='
                ) goto L0002;
                ctx->pos++;
                ctx->pos = p;
                goto L0001;
            L0002:;
                ctx->pos = p;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_xor_expr, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_xor_expr(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_and_expr, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '^'
            ) goto L0001;
            ctx->pos++;
            {
                int p = ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '='
                ) goto L0002;
                ctx->pos++;
                ctx->pos = p;
                goto L0001;
            L0002:;
                ctx->pos = p;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_and_expr, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_and_expr(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_shift_expr, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '&'
            ) goto L0001;
            ctx->pos++;
            {
                int p = ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '='
                ) goto L0002;
                ctx->pos++;
                ctx->pos = p;
                goto L0001;
            L0002:;
                ctx->pos = p;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_shift_expr, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_shift_expr(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_arith_expr, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            {
                int p = ctx->pos;
                int n = chunk->thunks.len;
                {
                    const char *s = ctx->buffer.buf + ctx->pos;
                    if (
                        pcc_refill_buffer(ctx, 2) < 2 ||
                        s[0] != '<' ||
                        s[1] != '<'
                    ) goto L0003;
                    ctx->pos += 2;
                }
                goto L0002;
            L0003:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                {
                    const char *s = ctx->buffer.buf + ctx->pos;
                    if (
                        pcc_refill_buffer(ctx, 2) < 2 ||
                        s[0] != '>' ||
                        s[1] != '>'
                    ) goto L0004;
                    ctx->pos += 2;
                }
                goto L0002;
            L0004:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0001;
            L0002:;
            }
            {
                int p = ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '='
                ) goto L0005;
                ctx->pos++;
                ctx->pos = p;
                goto L0001;
            L0005:;
                ctx->pos = p;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_arith_expr, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_arith_expr(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_term, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            {
                int p = ctx->pos;
                int n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '+'
                ) goto L0003;
                ctx->pos++;
                goto L0002;
            L0003:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '-'
                ) goto L0004;
                ctx->pos++;
                goto L0002;
            L0004:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0001;
            L0002:;
            }
            {
                int p = ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '='
                ) goto L0005;
                ctx->pos++;
                ctx->pos = p;
                goto L0001;
            L0005:;
                ctx->pos = p;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_term, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_term(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_factor, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            {
                int p = ctx->pos;
                int n = chunk->thunks.len;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '*'
                ) goto L0003;
                ctx->pos++;
                {
                    int p = ctx->pos;
                    if (
                        pcc_refill_buffer(ctx, 1) < 1 ||
                        ctx->buffer.buf[ctx->pos] != '*'
                    ) goto L0004;
                    ctx->pos++;
                    ctx->pos = p;
                    goto L0003;
                L0004:;
                    ctx->pos = p;
                }
                goto L0002;
            L0003:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '@'
                ) goto L0005;
                ctx->pos++;
                goto L0002;
            L0005:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '%'
                ) goto L0006;
                ctx->pos++;
                goto L0002;
            L0006:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                {
                    const char *s = ctx->buffer.buf + ctx->pos;
                    if (
                        pcc_refill_buffer(ctx, 2) < 2 ||
                        s[0] != '/' ||
                        s[1] != '/'
                    ) goto L0007;
                    ctx->pos += 2;
                }
                goto L0002;
            L0007:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '/'
                ) goto L0008;
                ctx->pos++;
                goto L0002;
            L0008:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0001;
            L0002:;
            }
            {
                int p = ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != '='
                ) goto L0009;
                ctx->pos++;
                ctx->pos = p;
                goto L0001;
            L0009:;
                ctx->pos = p;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_factor, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_factor(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            int p = ctx->pos;
            int n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '+'
            ) goto L0004;
            ctx->pos++;
            goto L0003;
        L0004:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '-'
            ) goto L0005;
            ctx->pos++;
            goto L0003;
        L0005:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '~'
            ) goto L0006;
            ctx->pos++;
            goto L0003;
        L0006:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_factor, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_power, &chunk->thunks, NULL)) goto L0007;
        goto L0001;
    L0007:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_power(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_atom_expr, &chunk->thunks, NULL)) goto L0000;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != '*' ||
            s[1] != '*'
        ) goto L0001;
        ctx->pos += 2;
    }
    {
        int p = ctx->pos;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '='
        ) goto L0002;
        ctx->pos++;
        ctx->pos = p;
        goto L0001;
    L0002:;
        ctx->pos = p;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_factor, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_atom_expr(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            s[0] != 'a' ||
            s[1] != 'w' ||
            s[2] != 'a' ||
            s[3] != 'i' ||
            s[4] != 't'
        ) goto L0001;
        ctx->pos += 5;
    }
L0001:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_atom, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_trailer, &chunk->thunks, NULL)) goto L0002;
        }
    L0002:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_atom(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            int p = ctx->pos;
            int n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '('
            ) goto L0004;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0004;
            {
                int p = ctx->pos;
                int n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_yield_expr, &chunk->thunks, NULL)) goto L0007;
                goto L0006;
            L0007:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_testlist_comp, &chunk->thunks, NULL)) goto L0008;
                goto L0006;
            L0008:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0005;
            L0006:;
            }
        L0005:;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0004;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ')'
            ) goto L0004;
            ctx->pos++;
            goto L0003;
        L0004:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '['
            ) goto L0009;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0009;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_testlist_comp, &chunk->thunks, NULL)) goto L0010;
        L0010:;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0009;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ']'
            ) goto L0009;
            ctx->pos++;
            goto L0003;
        L0009:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '{'
            ) goto L0011;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0011;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_dictorsetmaker, &chunk->thunks, NULL)) goto L0012;
        L0012:;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0011;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '}'
            ) goto L0011;
            ctx->pos++;
            goto L0003;
        L0011:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_string, &chunk->thunks, NULL)) goto L0013;
            goto L0003;
        L0013:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_number, &chunk->thunks, NULL)) goto L0014;
            goto L0003;
        L0014:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const char *s = ctx->buffer.buf + ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    s[0] != '.' ||
                    s[1] != '.' ||
                    s[2] != '.'
                ) goto L0015;
                ctx->pos += 3;
            }
            goto L0003;
        L0015:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const char *s = ctx->buffer.buf + ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    s[0] != 'N' ||
                    s[1] != 'o' ||
                    s[2] != 'n' ||
                    s[3] != 'e'
                ) goto L0016;
                ctx->pos += 4;
            }
            goto L0003;
        L0016:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const char *s = ctx->buffer.buf + ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    s[0] != 'T' ||
                    s[1] != 'r' ||
                    s[2] != 'u' ||
                    s[3] != 'e'
                ) goto L0017;
                ctx->pos += 4;
            }
            goto L0003;
        L0017:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const char *s = ctx->buffer.buf + ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 5) < 5 ||
                    s[0] != 'F' ||
                    s[1] != 'a' ||
                    s[2] != 'l' ||
                    s[3] != 's' ||
                    s[4] != 'e'
                ) goto L0018;
                ctx->pos += 5;
            }
            goto L0003;
        L0018:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0019;
        goto L0001;
    L0019:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_testlist_comp(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_namedexpr_test, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_star_expr, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_comp_for, &chunk->thunks, NULL)) goto L0005;
        goto L0004;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int i;
            for (i = 0;; i++) {
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != ','
                ) goto L0007;
                ctx->pos++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0007;
                {
                    int p = ctx->pos;
                    int n = chunk->thunks.len;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_namedexpr_test, &chunk->thunks, NULL)) goto L0009;
                    goto L0008;
                L0009:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_star_expr, &chunk->thunks, NULL)) goto L0010;
                    goto L0008;
                L0010:;
                    ctx->pos = p;
                    pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                    goto L0007;
                L0008:;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0007;
            }
        L0007:;
        }
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ','
        ) goto L0011;
        ctx->pos++;
    L0011:;
    L0004:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_trailer(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '('
        ) goto L0002;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_arglist, &chunk->thunks, NULL)) goto L0003;
    L0003:;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ')'
        ) goto L0002;
        ctx->pos++;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '['
        ) goto L0004;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0004;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_subscriptlist, &chunk->thunks, NULL)) goto L0004;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0004;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ']'
        ) goto L0004;
        ctx->pos++;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0005;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '.'
        ) goto L0005;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0005;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_subscriptlist(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_subscript, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_subscript, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ','
    ) goto L0002;
    ctx->pos++;
L0002:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_subscript(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0003;
    L0003:;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != ':'
        ) goto L0002;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0004;
    L0004:;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_sliceop, &chunk->thunks, NULL)) goto L0005;
    L0005:;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_sliceop(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_exprlist(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expr, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_star_expr, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0004;
            ctx->pos++;
            {
                int p = ctx->pos;
                int n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expr, &chunk->thunks, NULL)) goto L0006;
                goto L0005;
            L0006:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_star_expr, &chunk->thunks, NULL)) goto L0007;
                goto L0005;
            L0007:;
                ctx->pos = p;
                pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                goto L0004;
            L0005:;
            }
        }
    L0004:;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ','
    ) goto L0008;
    ctx->pos++;
L0008:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_testlist(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ','
    ) goto L0002;
    ctx->pos++;
L0002:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_dictorsetmaker(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            int p = ctx->pos;
            int n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0004;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ':'
            ) goto L0004;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0004;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0004;
            goto L0003;
        L0004:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                const char *s = ctx->buffer.buf + ctx->pos;
                if (
                    pcc_refill_buffer(ctx, 2) < 2 ||
                    s[0] != '*' ||
                    s[1] != '*'
                ) goto L0005;
                ctx->pos += 2;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expr, &chunk->thunks, NULL)) goto L0005;
            goto L0003;
        L0005:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0003:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
        {
            int p = ctx->pos;
            int n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_comp_for, &chunk->thunks, NULL)) goto L0007;
            goto L0006;
        L0007:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                int i;
                for (i = 0;; i++) {
                    if (
                        pcc_refill_buffer(ctx, 1) < 1 ||
                        ctx->buffer.buf[ctx->pos] != ','
                    ) goto L0009;
                    ctx->pos++;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0009;
                    {
                        int p = ctx->pos;
                        int n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0011;
                        if (
                            pcc_refill_buffer(ctx, 1) < 1 ||
                            ctx->buffer.buf[ctx->pos] != ':'
                        ) goto L0011;
                        ctx->pos++;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0011;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0011;
                        goto L0010;
                    L0011:;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        {
                            const char *s = ctx->buffer.buf + ctx->pos;
                            if (
                                pcc_refill_buffer(ctx, 2) < 2 ||
                                s[0] != '*' ||
                                s[1] != '*'
                            ) goto L0012;
                            ctx->pos += 2;
                        }
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_expr, &chunk->thunks, NULL)) goto L0012;
                        goto L0010;
                    L0012:;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0009;
                    L0010:;
                    }
                }
            L0009:;
            }
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0013;
            ctx->pos++;
        L0013:;
        L0006:;
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int p = ctx->pos;
            int n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0016;
            goto L0015;
        L0016:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_star_expr, &chunk->thunks, NULL)) goto L0017;
            goto L0015;
        L0017:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0014;
        L0015:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0014;
        {
            int p = ctx->pos;
            int n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_comp_for, &chunk->thunks, NULL)) goto L0019;
            goto L0018;
        L0019:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                int i;
                for (i = 0;; i++) {
                    if (
                        pcc_refill_buffer(ctx, 1) < 1 ||
                        ctx->buffer.buf[ctx->pos] != ','
                    ) goto L0021;
                    ctx->pos++;
                    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0021;
                    {
                        int p = ctx->pos;
                        int n = chunk->thunks.len;
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0023;
                        goto L0022;
                    L0023:;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_star_expr, &chunk->thunks, NULL)) goto L0024;
                        goto L0022;
                    L0024:;
                        ctx->pos = p;
                        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
                        goto L0021;
                    L0022:;
                    }
                }
            L0021:;
            }
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0025;
            ctx->pos++;
        L0025:;
        L0018:;
        }
        goto L0001;
    L0014:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_classdef(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            s[0] != 'c' ||
            s[1] != 'l' ||
            s[2] != 'a' ||
            s[3] != 's' ||
            s[4] != 's'
        ) goto L0000;
        ctx->pos += 5;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_name, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '('
    ) goto L0001;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_arglist, &chunk->thunks, NULL)) goto L0002;
L0002:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ')'
    ) goto L0001;
    ctx->pos++;
L0001:;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_suite, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_arglist(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_argument, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != ','
            ) goto L0001;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_argument, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ','
    ) goto L0002;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0002;
L0002:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_argument(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != '*' ||
                s[1] != '*'
            ) goto L0002;
            ctx->pos += 2;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '*'
        ) goto L0003;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0004;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                s[0] != ':' ||
                s[1] != '='
            ) goto L0004;
            ctx->pos += 2;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0004;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0005;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '='
        ) goto L0005;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0005;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0006;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_comp_for, &chunk->thunks, NULL)) goto L0007;
    L0007:;
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_comp_iter(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_comp_for, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_comp_if, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_sync_comp_for(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 3) < 3 ||
            s[0] != 'f' ||
            s[1] != 'o' ||
            s[2] != 'r'
        ) goto L0000;
        ctx->pos += 3;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_exprlist, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != 'i' ||
            s[1] != 'n'
        ) goto L0000;
        ctx->pos += 2;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_or_test, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_comp_iter, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_comp_for(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            s[0] != 'a' ||
            s[1] != 's' ||
            s[2] != 'y' ||
            s[3] != 'n' ||
            s[4] != 'c'
        ) goto L0001;
        ctx->pos += 5;
    }
L0001:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_sync_comp_for, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_comp_if(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 2) < 2 ||
            s[0] != 'i' ||
            s[1] != 'f'
        ) goto L0000;
        ctx->pos += 2;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test_nocond, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_comp_iter, &chunk->thunks, NULL)) goto L0001;
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_yield_expr(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        const char *s = ctx->buffer.buf + ctx->pos;
        if (
            pcc_refill_buffer(ctx, 5) < 5 ||
            s[0] != 'y' ||
            s[1] != 'i' ||
            s[2] != 'e' ||
            s[3] != 'l' ||
            s[4] != 'd'
        ) goto L0000;
        ctx->pos += 5;
    }
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            const char *s = ctx->buffer.buf + ctx->pos;
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                s[0] != 'f' ||
                s[1] != 'r' ||
                s[2] != 'o' ||
                s[3] != 'm'
            ) goto L0003;
            ctx->pos += 4;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_test, &chunk->thunks, NULL)) goto L0003;
        goto L0002;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_testlist_star_expr, &chunk->thunks, NULL)) goto L0004;
        goto L0002;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0001;
    L0002:;
    }
L0001:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_func_body_suite(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NEWLINE, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_TYPE_COMMENT, &chunk->thunks, NULL)) goto L0003;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NEWLINE, &chunk->thunks, NULL)) goto L0003;
    L0003:;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_INDENT, &chunk->thunks, NULL)) goto L0002;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_stmt, &chunk->thunks, NULL)) goto L0002;
        {
            int i;
            for (i = 0;; i++) {
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EQDENT, &chunk->thunks, NULL)) goto L0004;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_stmt, &chunk->thunks, NULL)) goto L0004;
            }
        L0004:;
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DEDENT, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_simple_stmt, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_name(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NAME, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NAME(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0002;
        ctx->pos++;
        ctx->pos = p;
        goto L0003;
    L0002:;
        ctx->pos = p;
        goto L0001;
    L0003:;
    }
L0001:;
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_number(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NUMBER, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NUMBER(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0002;
        ctx->pos++;
        ctx->pos = p;
        goto L0003;
    L0002:;
        ctx->pos = p;
        goto L0001;
    L0003:;
    }
L0001:;
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_string(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STRING, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_STRING(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0002;
        ctx->pos++;
        ctx->pos = p;
        goto L0003;
    L0002:;
        ctx->pos = p;
        goto L0001;
    L0003:;
    }
L0001:;
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NEWLINE(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0002;
        ctx->pos++;
        ctx->pos = p;
        goto L0003;
    L0002:;
        ctx->pos = p;
        goto L0001;
    L0003:;
    }
L0001:;
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule__(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0002;
        ctx->pos++;
        ctx->pos = p;
        goto L0003;
    L0002:;
        ctx->pos = p;
        goto L0001;
    L0003:;
    }
L0001:;
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_INDENT(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0002;
        ctx->pos++;
        ctx->pos = p;
        goto L0003;
    L0002:;
        ctx->pos = p;
        goto L0001;
    L0003:;
    }
L0001:;
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DEDENT(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0002;
        ctx->pos++;
        ctx->pos = p;
        goto L0003;
    L0002:;
        ctx->pos = p;
        goto L0001;
    L0003:;
    }
L0001:;
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_EQDENT(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0002;
        ctx->pos++;
        ctx->pos = p;
        goto L0003;
    L0002:;
        ctx->pos = p;
        goto L0001;
    L0003:;
    }
L0001:;
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ENDMARKER(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0001;
        ctx->pos++;
        ctx->pos = p;
        goto L0000;
    L0001:;
        ctx->pos = p;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_TYPE_COMMENT(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0002;
        ctx->pos++;
        ctx->pos = p;
        goto L0003;
    L0002:;
        ctx->pos = p;
        goto L0001;
    L0003:;
    }
L0001:;
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ENCODING(pglc_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    pcc_value_table__resize(ctx->auxil, &chunk->values, 0);
    pcc_capture_table__resize(ctx->auxil, &chunk->capts, 0);
    {
        int p = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0002;
        ctx->pos++;
        ctx->pos = p;
        goto L0003;
    L0002:;
        ctx->pos = p;
        goto L0001;
    L0003:;
    }
L0001:;
    return chunk;
}

pglc_context_t *pglc_create(void *auxil) {
    return pcc_context__create(auxil);
}

int pglc_parse(pglc_context_t *ctx, int *ret) {
    pcc_thunk_array_t thunks;
    pcc_thunk_array__init(ctx->auxil, &thunks, PCC_ARRAYSIZE);
    if (pcc_apply_rule(ctx, pcc_evaluate_rule_start, &thunks, ret))
        pcc_do_action(ctx, &thunks, ret);
    else
        PCC_ERROR(ctx->auxil);
    pcc_commit_buffer(ctx);
    pcc_thunk_array__term(ctx->auxil, &thunks);
    return pcc_refill_buffer(ctx, 1) >= 1;
}

void pglc_destroy(pglc_context_t *ctx) {
    pcc_context__destroy(ctx);
}


int main() {
    int ret;
    pglc_context_t *ctx = pglc_create(NULL);
    while (pglc_parse(ctx, &ret));
    pglc_destroy(ctx);
}

